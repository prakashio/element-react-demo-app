const stateData = {
  loading: false,
  notes: [
    {
      note:
        "The Summary component expects an array for ingredients and an array for steps.",
      rating: 5
    },
    {
      note:
        "It may make more sense to refactor our code to expect numbers instead, since the component doesn’t actually need arrays.",
      rating: 5
    }
  ]
};

export default stateData;
